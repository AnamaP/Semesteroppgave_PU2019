package logikk;

import javafx.scene.control.TableColumn;
import javafx.scene.control.cell.TextFieldTableCell;
import klasser.Arbeidsgiver;

public class EditCellHelper {

    /* Implementing Cell Editing

    tcTlf.setCellFactory(TextFieldTableCell.<TabellVikariater>forTableColumn());
    tcTlf.setOnEditCommit(
        (
    TableColumn.CellEditEvent<Arbeidsgiver, String> t) -> {
            (t.getTableView().getItems().get(
                t.getTablePosition().getRow())
                ).setTlf(t.getNewValue());
    });*/
}
